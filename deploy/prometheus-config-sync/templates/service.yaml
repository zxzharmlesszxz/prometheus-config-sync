apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-config-sync.fullname" . }}
  labels:
    {{- include "prometheus-config-sync.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: metrics
      port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "prometheus-config-sync.selectorLabels" . | nindent 4 }}
